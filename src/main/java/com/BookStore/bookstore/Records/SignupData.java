package com.BookStore.bookstore.Records;

public record SignupData(String firstName,
                         String lastName,
                         String email,
                         String password)
{};